apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
  labels:
    app: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        # MySQL 9.6.0 for ARM64 (aarch64) architecture
        # From Alibaba Cloud Registry
        # This image is compatible with ARM64 servers
        # Source: registry.cn-hangzhou.aliyuncs.com/public_hjj_images/mysql9.6.0:arm64
        image: registry.cn-hangzhou.aliyuncs.com/public_hjj_images/mysql9.6.0:arm64
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "123456"
        args:
        - "--lower-case-table-names=1"
        - "--character-set-server=utf8mb4"
        - "--collation-server=utf8mb4_general_ci"
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-storage
        persistentVolumeClaim:
          claimName: mysql-pvc
